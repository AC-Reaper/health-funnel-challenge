# 00 — Product Research

> **Purpose.** Capture what we learned from the reference competitor
> (BetterMe Pilates Quiz Funnel) and any other product references. This
> doc grounds the requirements in `01-requirements.md` and the scope
> decisions in `02-architecture.md`. Not a feature wishlist — only
> observations that change a design decision belong here.
>
> **Owner.** Claude. Updated on Day 1, then frozen unless a new reference
> is introduced.
>
> **Day-1 guidance.** Keep this short. Bullet observations, not essays.
> If an observation does not change a requirement (`01`) or a design
> choice (`02`/`03`/`04`), do not write it down.

## 1. Reference product

- Name: BetterMe Pilates Quiz Funnel
- URL: https://betterme-pilates.com/first-page-brand-palette?flow=2117
- Why we look at it: brief instructs us to model the **data flow**
  (which steps produce which data, what is persisted, what changes
  before vs after payment), not the visual design.

## 2. Funnel steps observed

*(To be filled in Day 1 from a walkthrough of the reference flow.
Capture: step order, input type per step, copy patterns that build
trust, where the paywall lands, what the result page promises.)*

## 3. Data we infer is persisted

*(To be filled in Day 1. Distinguish: must be persisted to support
resume; must be persisted to compute the result; must be persisted to
gate the paid view.)*

## 4. Pre-paywall vs post-paywall surface

*(To be filled in Day 1. What does a non-paying user see? What changes
after `/pay`?)*

## 5. Decisions we make differently, and why

*(To be filled in Day 1. The brief explicitly invites us to make
"我们认为更合理" judgement calls — record each here so reviewers can
trace the reasoning back to a source.)*

## 6. References

*(Links to any other products consulted, screenshots if useful.)*
