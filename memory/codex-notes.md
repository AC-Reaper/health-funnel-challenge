# Codex Notes

## Notes

- Initialized repository architecture.
- 2026-05-18: Codex accepted Reviewer / QA / Architecture Critic role and recorded baseline readiness findings in `reviews/review-000-baseline-readiness.md`.
- 2026-05-18: Reviewed Claude's `docs/02-architecture.md` draft from a 5-day interview-delivery perspective. Findings recorded in `reviews/review-001-architecture.md`.
- 2026-05-18: Reviewed Claude's governance/scaffold rewrite (`AGENTS.md`, `PROJECT_BRIEF.md`, `README.md`, memory/docs skeletons), accepted ADR-001…010 per Owner request, and unblocked Day-1 start at T-101.
- 2026-05-18: Read `memory_sample.md`, aligned memory files to the sample's card-style format, recorded Owner decisions for Q-002/Q-003/Q-006, and added ADR-011…013 for branch workflow, payment replay semantics, and demo/calculator defaults.
- 2026-05-18: Reviewed `feature/db-schema` and wrote `reviews/review-003-db.md`. Main blocker: `result.bmi decimal(4,2)` cannot store all validation-accepted boundary BMI values; branch should not merge until widened.
- 2026-05-18: Re-reviewed `feature/db-schema` at `cc40d3d`. Original review-003 Blocking and Important findings are resolved; only a docs-only ER diagram type-label nit remains.
- 2026-05-18: Reviewed `feature/session-progress-api` and wrote interim `reviews/review-002-api.md`. Main blocker: `POST /api/v1/sessions` skips Zod/body validation despite ADR-005.
- 2026-05-18: Re-reviewed `feature/session-progress-api` at `11098e3`. Body validation, server-only boundaries, stale payment code, and DTO docs are fixed locally; live `POST /sessions` happy-path smoke remains pending on T-102.
- 2026-05-18: Scanned `db992ab`; live Supabase smoke coverage is sufficient to close review-002 I004 for T-101/T-104/T-105. Branch is mergeable from the session-foundation review perspective; step API review still needed after T-202.
- 2026-05-18: Reviewed `feature/funnel-persistence-api` at `f1ae3b3`. `typecheck`, 67 Vitest tests, and build pass, but review-002 is reopened for step API findings: inherited step-key guard bug, weight/main_goal edit coherence, stale session cache, and missing route-level regression tests.
- 2026-05-18: Re-reviewed `feature/funnel-persistence-api` closeout at `36f8830`. B002, I005, I006, and N004 are fixed in code/tests/docs; I007 is accepted as resolved-partial with pure helper tests plus recorded live Supabase smoke. `typecheck`, 108 Vitest tests, and build pass. Branch is mergeable from the step-API review perspective.
- 2026-05-19: Reviewed Day-3 `feature/assessment-result-api` at `d79959c` and wrote `reviews/review-006-day3.md`. `typecheck`, 160 Vitest tests, and build pass. Review is Open with 1 Blocking (missing committed idempotency regression tests for `/submit` and `/pay`), 2 Important (submit-time cross-field coherence; 52-week curve truncation), and 3 Nice-to-have.
- 2026-05-19: Re-reviewed Day-3 closeout at `7b17949`. `typecheck`, 175 Vitest tests, and build pass. B001/I001/I002/N001/N002 are resolved; N003 is accepted as a Day-4 `/pay` UX deferral. `feature/assessment-result-api` is mergeable from the Day-3 review perspective.
- 2026-05-19: Ran deployed browser smoke against `https://project-u415a.vercel.app/` and wrote `reviews/review-007-browser-smoke.md`. The public URL still serves the pre-frontend placeholder, `/funnel` is 404, and the requested quiz → pay → full-result browser loop cannot start. No-cookie `/pay` redirects to `/` as expected.
- 2026-05-19: Re-smoked preview deployment `https://project-u415a-oafjf8eba-jackz1.vercel.app/`. Root and `/pay` both redirect to Vercel login, so the browser flow is still blocked before the app loads. Added review-007 B002.
- 2026-05-19: Re-smoked the same preview after protection was removed. Full browser flow passes: no-cookie `/pay` redirects to `/`; two-step incomplete session shows `Finish the quiz first`; six-step quiz reaches `/results` teaser; unlock goes to `/pay`; `Pay $9.99` returns to full result with calorie target, predicted date, weekly curve, and algorithm version. review-007 B002 resolved for preview; B001 remains only for the stale production alias if that URL is submitted.
- 2026-05-19: Re-smoked production alias `https://project-u415a.vercel.app/` after main deploy. Root serves Day-4 UI; no-cookie `/pay` redirects to `/`; six-step quiz reaches teaser; unlock goes to `/pay`; `Pay $9.99` returns to full result. review-007 B001 is resolved and review-007 is fully Resolved.
- 2026-05-19: Wrote final `reviews/review-004-final.md` for `feature/day5-hardening` at `1d55b1b`. `typecheck`, 181 tests, `db:validate`, and build pass. No Blocking findings; Important items are final docs drift around `step_event`, cookie TTL/API docs, and delivery checklist/README readiness; one Nice-to-have asks for lightweight `step_event` write proof.
- 2026-05-19: Re-reviewed `feature/day5-hardening` at `495b7ed`. `typecheck`, 184 tests, `db:validate`, and build pass. T-501/I002 cookie-doc cleanup and N001 `StepsTxOps` seam are verified; README/docs/03/docs/04/docs/07 are in good shape. One Important remains in `docs/02-architecture.md`: stale ADR heading, Postman-in-scope claim, old schema field types, and obsolete open-followups.
- 2026-05-19: Final re-reviewed `feature/day5-hardening` at `f2b37f8`. `typecheck`, 184 tests, `db:validate`, and build pass. `docs/02-architecture.md` now clears I004; all review-004 findings are resolved. Branch is ready to merge to `main` from the reviewer perspective.
- 2026-05-19: Reviewed `feature/frontend-polish` at `8f6f80a` and wrote `reviews/review-008-frontend-polish.md`. `typecheck`, 184 tests, build, and `db:validate` pass. Preview smoke on `https://project-u415a-ljz3rs2mm-jackz1.vercel.app/` verifies auto-advance happy path, Back-then-edit `viewStep`, LockedPreview teaser, pay → full report, and 360px root/full-result sanity; same-branch local 360px funnel sanity used because preview browser held a paid httpOnly cookie. No Blocking findings; 1 Important remains: auto-advance single-choice components do not clear `selecting` after failed save.
- 2026-05-19: Re-reviewed `feature/frontend-polish` at `c974fbb`. `typecheck`, 184 tests, build, and `db:validate` pass. Code review verifies review-008 I001 is fixed with `wasPendingRef` true→false reset in Gender/MainGoal/Activity; Preview smoke on `https://project-u415a-doh7rtyig-jackz1.vercel.app/` verifies auto-advance, locked teaser, pay, full result, and 360px paid-result sanity. review-008 is Resolved and the branch is mergeable from the frontend-polish review perspective.
- 2026-05-19: Reviewed `feature/security-hardening` at `d6e4c66` and wrote `reviews/review-009-security-hardening.md`. `typecheck`, 202 tests, build, `db:validate`, and `git diff --check` pass. Preview smoke on `https://project-u415a-nvhyy1s1z-jackz1.vercel.app/` verifies healthz, no-Origin session creation, cross-Origin 403, missing / non-ASCII Idempotency-Key 400, cross-Origin /pay 403, and root → funnel load. No Blocking findings; 1 Important remains: `docs/08` falsely claims the only raw query is a SELECT 1 warm-up; 1 Nice-to-have notes same-origin is currently host-only rather than scheme-aware.
- 2026-05-19: Re-reviewed `feature/security-hardening` at `adafa91`. `typecheck`, 206 tests, build, `db:validate`, and `git diff --check` pass. N001 is resolved by conditional scheme checking via `x-forwarded-proto` plus 4 new tests. I001 remains open: `docs/08` and `reviews/resolved-review-items.md` now claim two `$queryRaw` callsites including a `lib/db.ts` SELECT 1 warm-up, but current app/lib/prisma/tests grep returns only `lib/payment.ts:183`; the docs proof still needs to match the repo exactly.
- 2026-05-20: Third re-reviewed `feature/security-hardening` at `bcb4f2a`. `typecheck`, 206 tests, build, `db:validate`, and `git diff --check` pass. `rg -n '\$queryRaw\|\$executeRaw' app lib prisma tests --glob '!node_modules' --glob '!package-lock.json'` returns exactly one site (`lib/payment.ts:183`), and `docs/08` + `reviews/resolved-review-items.md` now describe that exact surface. review-009 is Resolved and the branch is mergeable from the security-hardening review perspective.
- 2026-05-20: Reviewed `feature/delivery-compliance-hardening` at `38f6911` and wrote `reviews/review-010-delivery-compliance.md`. `typecheck`, 210 tests, build, `db:validate`, diff-check, and a live production paid-session cURL smoke pass. No Blocking findings; I001 flags stale final-checklist review state, and N001 flags `docs/04` error-model prose that still says `fields` appears only on `VALIDATION_ERROR`.
- 2026-05-20: Re-reviewed `feature/delivery-compliance-hardening` at `a14b90f`. I001 and N001 are resolved: `docs/07` + README review-state wording is current through review-010, and `docs/04` now documents every current `fields` shape. `typecheck`, 210 tests, build, `db:validate`, and diff-check pass. review-010 is Resolved and the branch is mergeable from the delivery-compliance perspective.
- 2026-05-20: Reviewed `feature/production-hardening` at `2dcbee6` and wrote `reviews/review-011-production-hardening.md`. `typecheck`, 222 tests, build, `db:validate`, diff-check, `npm audit --omit=dev`, and local `next start` header smoke pass. No Blocking findings. I001 flags the body-size cap proof: docs say actual byte length, but fallback checks `text.length`; I002 flags the unrecorded Next 14→15 ADR-level decision. N001 asks `APP_ORIGIN` to reject non-http(s) schemes, and N002 notes the Next build multiple-lockfile warning.
- 2026-05-20: Re-reviewed `feature/production-hardening` at `06817a5`. I001/I002/N001/N002 are resolved: byte cap now uses `Buffer.byteLength(text, "utf8")` plus a multibyte 413 regression; ADR-015 records the Next 15.5.18 patch baseline; `APP_ORIGIN` rejects non-http(s) schemes; `outputFileTracingRoot` removes the Next 15 multiple-lockfile warning. `typecheck`, 224 tests, build, `db:validate`, diff-check, `npm audit --omit=dev`, and `npm ls next postcss` pass. review-011 is Resolved and the branch is mergeable from the production-hardening review perspective.
- 2026-05-20: Reviewed `feature/security-polish` at `ba15dac` and wrote `reviews/review-012-security-polish.md`. `typecheck`, 224 tests, build, `db:validate`, diff-check, `npm audit --omit=dev`, and local header smoke pass. `X-Powered-By` is absent on `/api/v1/healthz`, `/api/v1/sessions/me`, and `/pay`; existing security headers remain. No Blocking findings. I001 flags README headline/status drift: it still says 222 tests and reviews 000…010 even though review-011 is resolved and the suite is 224.
- 2026-05-20: Reviewed `feature/landing-cta` at `f3ea061` and wrote `reviews/review-013-landing-cta.md`. `typecheck`, 228 tests, build, `db:validate`, and `git diff --check main...HEAD` pass. Local cookie-jar smoke verifies landing CTA states: no cookie → Start; empty draft → Start; draft with saved gender → Continue; submitted session → View your results. No Blocking, Important, or Nice-to-have findings. Also corrected `docs/06-review-log.md` so review-012 is marked Resolved, matching `reviews/review-012-security-polish.md` and `reviews/resolved-review-items.md`.
- 2026-05-21: Reviewed `feature/rate-limit` at `b2403a1` and wrote `reviews/review-014-rate-limit.md`. `typecheck`, 240 tests, build, `db:validate`, diff-check, raw-query grep, and preview `/pay` rate-limit smoke pass. No Blocking or Important findings. N001 remains: `identityHash()` comment says "Salted SHA-256" without a salt/pepper, and `docs/08` cites the sole `$queryRaw` site as `lib/payment.ts:183` while current grep returns `lib/payment.ts:200`.
