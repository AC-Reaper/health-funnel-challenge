# Claude Notes

## Notes

- 2026-05-18 — Took on Primary Implementer role. Wrote `CLAUDE.md` collaboration protocol.
- 2026-05-18 — Drafted technical proposal (architecture v1) in `docs/02-architecture.md`. Locked stack proposal at Next.js 14 App Router + TypeScript + Prisma + Supabase + Vercel, anonymous signed-cookie sessions, Zod validation, mocked `/pay` with idempotency key. Six decisions (D1–D6) were awaiting Owner sign-off; later promoted to ADR-001…010 — see `memory/open-questions.md` Q-001.
- 2026-05-18 — Logged Day-1 through Day-5 tasks (T-1xx … T-5xx) in `memory/task-board.md`. Codex review triggers scheduled at end of Day 1, Day 2, and Day 5.
- 2026-05-18 — Partially addressed Codex baseline review item B002 (architecture spec). Did not yet address B001 (product/requirements), B003 (DB schema), B004 (API contracts), B005 (runnable code), I001–I004 — those are scheduled on subsequent days. Tracked in `reviews/resolved-review-items.md`.
- 2026-05-18 — Processed `reviews/review-001-architecture.md`. Accepted nearly every point: payment routes renamed to `/pay` + `POST /api/v1/pay`; collapsed `subscription`+`payment_event` into `session.entitlement_status`+`paid_at` + single `payment` table; deferred `step_event` to optional Day-5; fixed step progress to first-incomplete-step rule; dropped pre-seeded paid sessionId in favour of cookie-jar cURL walkthrough; switched UUIDv7 → `crypto.randomUUID()`; cut serverless in-memory rate limiting; filled `docs/04-api-design.md`. Recorded as ADR-007…010 in `memory/decisions.md`. Architecture rewritten as v2. Q-004/Q-005 resolved by the revision; Q-006 added.
- 2026-05-18 — Scaffold-upgrade pass per Owner request. Rewrote `AGENTS.md`, `PROJECT_BRIEF.md`, `README.md`. Converted `memory/decisions.md` to ADR style. Converted `memory/task-board.md` to 4-column format keeping all T-101…T-505 ids. Added purpose headers + skeleton outlines to the still-TBD docs and to `memory/shared-memory.md`. No application code touched.
- 2026-05-18 — Processed `reviews/review-005-governance-scaffold.md` (0 Blocking, 5 Important, 2 Nice-to-have). Adopted all 7. Fixes applied: (I001) unchecked API row in `docs/07`; (I002) added "risk reason" requirement to `AGENTS.md` §5 step 3; (I003) collapsed `PROJECT_BRIEF.md` §3 from 5 deliverables to 4; (I004) added explicit "decide by end of Day 2" deadline + Codex recommendation to Q-006 in `memory/open-questions.md`; (I005) replaced D1–D6 "awaiting sign-off" language in `docs/02` §0 with an Accepted ADR-001…010 index; (N001) reworded T-106/T-204/T-505 ownership in `memory/task-board.md` to "Claude requests; Codex writes"; (N002) added Day-1 short-doc guidance to `docs/00` and `docs/01`.
- 2026-05-18 — Reformatted memory files per Owner-provided `memory_sample.md`: `shared-memory.md`, `decisions.md`, `open-questions.md`, `task-board.md`, `reviews/resolved-review-items.md` now use heading-per-entry style with bare-label sections (Status / Context / Decision / Reason / Consequences for ADRs; Status / Decision / Reason for Q-NNN). Renamed `Q001…Q006` → `Q-001…Q-006` for sample consistency; updated cross-refs in `docs/02-architecture.md`, `README.md`, `memory/shared-memory.md`. Kept `T-NNN` ids (referenced by ADRs and reviews; renaming would break links). Codex-owned review files (`review-000`, `review-001`, `review-005`) left untouched; their original B001/I001/N001 ids preserved as historical record.
- 2026-05-18 — `git init -b main`; baseline commit `chore: initialise scaffold and design docs` covers all of `AGENTS.md`, `CLAUDE.md`, `PROJECT_BRIEF.md`, `README.md`, the docs/ tree, the memory/ tree, and the reviews/ tree. Then branched to `feature/db-schema` per ADR-011.
- 2026-05-18 — `feature/db-schema` shipped: `.gitignore` (already on `main` from baseline), `.nvmrc=20`, `.env.example` (DATABASE_URL/DIRECT_URL/SESSION_COOKIE_SECRET with Supabase-shaped comments), `.env` (placeholder, git-ignored), `package.json` (prisma 5.22, @prisma/client 5.22, typescript 5.5, @types/node 20; scripts db:generate/format/validate/deploy/reset), `tsconfig.json` (strict + noUncheckedIndexedAccess + @/lib/@/app paths), `prisma/schema.prisma` (4 models, 8 native enums, UUIDv4 via gen_random_uuid, decimal precisions, JSONB, snake_case via @@map / @map throughout), `prisma/migrations/migration_lock.toml`, `prisma/migrations/20260518000000_init/migration.sql` (generated via `prisma migrate diff --from-empty --to-schema-datamodel --script`, prepended with `CREATE EXTENSION IF NOT EXISTS pgcrypto;`). Filled `docs/03-database-design.md` (ER Mermaid, per-table columns, enum list, index rationale, migration runbook). Refreshed `README.md` setup section. Local verification: `npm install` exit 0, `prisma format` formatted only (no semantic diff), `prisma validate` clean, `prisma generate` produced the client. Live `prisma migrate deploy` deferred to T-102.
- 2026-05-18 — Codex re-reviewed `feature/db-schema` at cc40d3d: 0 Blocking, 0 Important, 1 Nice-to-have (N004: ER diagram type labels stale). Fixed in `docs/03-database-design.md` Mermaid block (`decimal_5_2` / `varchar_128` / `char_3`). Branch is now mergeable to `main`.
- 2026-05-18 — Merged `feature/db-schema` into `main` with `--no-ff` (merge commit `2a56382`). Deleted the feature branch. Started `feature/session-progress-api`.
- 2026-05-18 — `feature/session-progress-api` shipped T-101 / T-104 / T-105. Files: `next.config.mjs`, `next-env.d.ts`, `app/layout.tsx`, `app/page.tsx`, `app/api/v1/healthz/route.ts`, `app/api/v1/sessions/route.ts`, `app/api/v1/sessions/me/route.ts`, `lib/db.ts`, `lib/env.ts`, `lib/session.ts`, `lib/api/errors.ts`, `lib/api/request-id.ts`. Updated: `package.json` (next ^14.2.15 + react ^18.3.1 + zod ^3.23.8 + types), `tsconfig.json` (Next plugin + `.next/types/**/*.ts` include), `.gitignore` (`tsconfig.tsbuildinfo`). 6 commits, all Conventional Commits per ADR-011. Verified: `tsc --noEmit`, `next build`, and runtime cURL against `npm run start` for `/healthz` (200), `/sessions/me` no-cookie (401 NO_SESSION envelope), `/sessions/me` tampered-cookie (401 NO_SESSION). DB-touching paths (`POST /sessions` happy path, `GET /sessions/me` with valid sid) deferred to T-102 (Supabase). `npm audit` flags 4 Next 14 advisories (Image Optimization API, Pages Router i18n, WebSocket upgrades, RSC cache poisoning) — none affect us; staying on Next 14.2.x rather than break-major to Next 16.
- 2026-05-19 — Processed `reviews/review-006-day3.md` (1 Blocking, 2 Important, 3 Nice-to-have). Adopted 5/6 on `feature/assessment-result-api`. B001: extracted `SubmitTxOps` (`lib/result-repo.ts`) and `PaymentTxOps` (`lib/payment.ts`) seams + pure `runSubmitTransaction`/`runPaymentTransaction` orchestrators + thin Prisma adapters; new `tests/lib/result-repo.test.ts` (4 cases) and 4 new state-machine cases in `tests/lib/payment.test.ts` exercise the contracts via in-memory fakes modelling the two unique constraints + P2002 throw. I001: moved `checkWeightCoherence` to `lib/health/coherence.ts` (`lib/assessment.ts` re-exports for backward compat); `FULL_ASSESSMENT_SCHEMA.superRefine` runs the same rule; `/submit` rejects incoherent rows pre-`compute()`; 5 new schema tests. I002: `computeCurveAndDate` no longer snaps the final point to `target` when the curve is truncated past `MAX_CURVE_WEEKS`; new boundary test (250 → 175, exactly 30% delta, truncated at week 52 to 224 kg). N001: `docs/04 §5` Test invariant now lists `algorithmVersion`. N002: duplicate Day-3 row removed from `README.md`. N003: deferred to T-401/T-402 per Codex's own suggestion; recorded in `resolved-review-items.md`. 7 Conventional Commits. 166 → 175 tests, all green. `tsc --noEmit` + `next build` (9 routes) clean.
- 2026-05-19 — Merged `feature/funnel-persistence-api` into `main` (`--no-ff`, merge commit `6ef135d`'s parent). Deleted feature branch. Created `feature/assessment-result-api` and shipped Day-3 in 7 Conventional Commits:
  - **T-301** `lib/health/calculator.ts` (pure, deterministic, versioned `v1.0.0-mifflin`): BMI banding, Mifflin-St Jeor BMR, TDEE with activity factor, daily calorie target with gender-specific floors, weekly curve at ±0.5 kg/week (build_muscle gain uses 0.25 kg/week), `predictedTargetDate=null` + `plan.note="consult_professional"` short-circuit when `|target − current|/current > 0.30`. `lib/validation/assessment.ts` composes the per-step schemas into `FULL_ASSESSMENT_SCHEMA` for /submit's belt-and-suspenders re-validation.
  - **T-302** `lib/result-repo.ts` with `submitAssessment` running a single `db.$transaction` — INSERT result (UNIQUE session_id; P2002 racer recovers by reading existing row) + UPDATE session.status='submitted' + submitted_at. `app/api/v1/sessions/me/submit/route.ts` returns 422 INCOMPLETE_ASSESSMENT with `fields.missingSteps: StepKey[]`, 500 on the submitted-without-result invariant violation, and 200 with the canonical envelope on success; idempotent replays return the existing resultId.
  - **T-303** `lib/serializers/result.ts` — two distinct DTO types (TeaserResultDTO, FullResultDTO) with no shared base; `serializeTeaser` cannot emit paid-only fields by construction. Headlines vary by BMI category via a const map. `app/api/v1/results/me/route.ts` returns 409 NOT_SUBMITTED before submit and branches on `session.entitlementStatus`. `tests/lib/serializers/result.test.ts` has the leak invariant: JSON.stringify(teaser) must not contain `dailyCaloriesKcal` / `predictedTargetDate` / `curvePoints` / `"plan"` / `algorithmVersion`.
  - **T-304** `lib/payment.ts` exports the pure `decidePaymentAction(session, existingForKey)` decision tree (4 cases: same_key_replay / already_paid_noop / same_key_pre_paid / insert_and_flip) plus the transactional `processPayment` that uses `SELECT … FOR UPDATE` to serialize concurrent /pay calls per session. `app/api/v1/pay/route.ts` validates `Idempotency-Key` via `z.string().min(1).max(128)`, parses an empty body via `parseJsonBody`, and returns the docs/04 §7 envelope.
  - Minimal `/pay` browser page + `PayButton` client component (`crypto.randomUUID()` per click → POST /api/v1/pay → redirect /results on success) and `/results` placeholder server page (will be replaced by T-401's polished UI on Day 4).
- Tests jumped 108 → 160 (calculator boundary matrix, FULL_ASSESSMENT_SCHEMA composition, teaser leak invariant, full DTO shape, decidePaymentAction tree). `tsc --noEmit` clean, `next build` clean (9 routes), live cookie-jar smoke against Supabase covered: /submit before session 401, /submit empty 422 INCOMPLETE_ASSESSMENT with all 6 missingSteps, /results before submit 409 NOT_SUBMITTED, full 6-step walk + /submit 200, /submit replay returns same resultId, /results teaser passes leak check, /pay without Idempotency-Key 400, /pay first call 200 paid, /pay same key replay returns same paymentId, /pay new key against already-paid silently no-ops (Prisma findMany confirms exactly one payment row), /results full after paid returns kind=full with all paid fields.
- 2026-05-18 — Processed step-API review section of `reviews/review-002-api.md` (1 Blocking, 3 Important, 1 Nice-to-have). All 5 adopted (one as partial). B002 (inherited keys bypass `isStepKey`) → switched to `Object.hasOwn` + dedicated tests. I005 (main_goal flip leaves incoherent state) → new `checkMainGoalChange` pure helper + route branch; 422 envelope tells the user to PATCH `/steps/weight` first. I006 (stale `session.current_step` / `updated_at`) → PATCH now runs assessment upsert + `session.update({ currentStep, ...})` inside `db.$transaction`; `@updatedAt` refreshes the timestamp. I007 (no route-level state-machine tests) → extracted `projectAssessment` / `stepIsFilled` / `firstMissingPrereq` to `lib/assessment.ts` and added 41 tests there; full route-handler integration tests (would need a Prisma mock layer or test-DB runtime) deferred and documented in `resolved-review-items.md`. N004 (build_muscle implicit) → doc note in `docs/04 §3` pinning "any direction" + dedicated test block. Tests 67 → 108. Live cookie-jar smoke confirms B002 (`toString` / `__proto__` → 400), I005 (lose_weight+80→70 then mainGoal=gain_weight → 422 with corrective message), I006 (`psql` snapshot shows `current_step="activity"` and `updated_at` 54s ahead of `created_at`).
- 2026-05-18 — Merged `feature/session-progress-api` into `main` (`--no-ff`, merge commit `0bda115`). Deleted feature branch. Started `feature/funnel-persistence-api`. Shipped T-201 (`lib/validation/steps.ts` — 6 `.strict()` Zod schemas per StepKey, mirroring docs/04 §3 bounds), T-202 (`lib/assessment.ts` with `upsertAssessmentField` + pure `checkWeightCoherence`; `app/api/v1/sessions/me/steps/[stepKey]/route.ts` PATCH handler with ordering: cookie→401, session→401, status=submitted→409, isStepKey→400, parseJsonBody→400/422, projectAssessment + first-incomplete-step→409, weight×main_goal coherence→422, upsertAssessmentField, return canonical SessionDTO; `docs/04 §3` aligned). T-203 (Vitest 4 wired with `tests/setup.ts` env defaults + `tests/_shims/server-only.ts` to neutralise the Next-only `server-only` guard in Node; 4 test files / 67 tests / all green — covers cookie sign/verify and failure modes, computeCurrentStep, every step schema's bounds incl. integer-only / `.strict()` extra-key rejection, parse-body envelope behaviour). Closes review-002 N003 by ride-along. Five Conventional Commits. Live cookie-jar smoke against Supabase covered 13 paths incl. all four 4xx envelopes from spec.
- 2026-05-18 — T-102 done. Owner provisioned Supabase (region `aws-1-us-east-1`); URLs both on the pooler host (port 6543 for `DATABASE_URL` with `?pgbouncer=true`, port 5432 for `DIRECT_URL`). Password had a `/` so URL-encoded to `%2F`; written to local `.env` only (gitignored, never committed). `npm run db:deploy` clean on first try, applied `20260518000000_init`. Introspection: 4 tables (`session`, `assessment`, `result`, `payment`) + `_prisma_migrations`; 8 native enums; 3 `payment` indexes incl. `payment_one_success_per_session_idx` as the ADR-012 backstop; FK delete actions `c/c/r` (CASCADE on assessment+result, RESTRICT on payment) all match the spec. Smoke against live DB: POST /sessions creates real UUID row + sets 159-char signed cookie; second POST returns same sessionId (reuse); GET /sessions/me with that cookie returns the canonical DTO; deleting the row via Prisma then GET /sessions/me with the same valid cookie returns `401 NO_SESSION`. All four DB-touching paths from review-002 I004 verified end-to-end. `feature/session-progress-api` now ready for Codex re-review.
- 2026-05-18 — Processed `reviews/review-002-api.md` (1 Blocking, 4 Important, 3 Nice-to-have). Adopted 7/8 in two commits on the same branch. (B001) Added `lib/api/parse-body.ts` and wired `POST /api/v1/sessions` through `z.object({}).strict()`. (I001) Added `import "server-only"` to `lib/session.ts`/`lib/db.ts`/`lib/env.ts` and extracted `computeCurrentStep` + `STEP_ORDER` to a new pure `lib/progress.ts` so a future client component can import the helper without dragging server modules in. (I002) Dropped `ALREADY_PAID` from `ERROR_CODES` to keep the lib aligned with ADR-012's silent no-op. (I003) Widened `ErrorFields` to `Record<string, string | string[]>` for the documented `missingSteps` shape on `/submit`. (I004) Tightened README + task-board wording — T-105 is "code shipped, awaits DB smoke + Codex re-review" not "shipped/done". (N001) Updated `docs/04` so `createdAt` + `answers` are canonical on both `POST /sessions` and `GET /sessions/me` responses. (N002) Updated README env comment to flag `SESSION_COOKIE_SECRET` required from this branch onward, with an `openssl rand -base64 48` hint. (N003) Unit tests for `signCookie` / `verifyCookie` / `computeCurrentStep` deferred to T-203 per Codex's own suggestion. cURL matrix re-verified: wrong content-type / malformed JSON → 400 BAD_REQUEST; unknown field → 422 VALIDATION_ERROR with `fields._`; valid `{}` → 500 INTERNAL_ERROR with structured log (no Supabase yet).
- 2026-05-18 — Processed `reviews/review-003-db.md` (1 Blocking, 4 Important, 3 Nice-to-have). All 8 adopted. Schema changes on the same branch: (B001) `Result.bmi` → `Decimal(5,2)`; (I001) added SQL-only partial unique index `payment_one_success_per_session_idx ON payment(session_id) WHERE status='succeeded'` as DB backstop for ADR-012; (I002) `Session.updatedAt` and `Assessment.updatedAt` → Prisma `@updatedAt`; (I003) `Payment.idempotencyKey` → `VarChar(128)`, `Payment.currency` → `Char(3)`. Doc changes: (I004) API `paymentId` example switched from `p_01HXY…` to UUID shape; (N001) README status reworded to "DB schema/migration shipped"; (N002) FK rationale reworded to "prevent accidental deletes from erasing audit data"; (N003) migration runbook reworded to "matches Prisma's diff output, with pgcrypto prepended". Regenerated migration via `prisma migrate diff`, re-prepended pgcrypto, re-appended the partial unique index. `prisma format`, `prisma validate`, `prisma generate` all pass.
